#!/bin/sh

case $BLOCK_BUTTON in
	1) xterm -class fullscreen -e selectlayout ;;
	3) xterm -e arandr ;;
esac

icon="&#xf26c;"

printf " %s %s \\n" "$icon" "Displays"
printf " %s \\n" "Displays"
echo "$(get_x_color orange400 color3)"
