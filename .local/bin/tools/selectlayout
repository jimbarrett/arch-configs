#!/bin/sh

FILE=~/.cache/screenlayouts/config

PS3='Select a layout: '
options=($(awk '{print $1}' $FILE))
select opt in "${options[@]}"
do
	layout=$opt
	break
done

seld=$(grep -Po "$layout \K[^\"]+" "$FILE")

xterm -e "$seld"
